---
- name: Destroy
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Remove instance config
      ansible.builtin.file:
        path: "{{ molecule_instance_config }}"
        state: absent
